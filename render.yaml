services:
  - type: web
    name: firequiz
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm ci && npm --prefix server ci && npm --prefix client ci && npm run build
    startCommand: npm --prefix server start
    healthCheckPath: /api/health
